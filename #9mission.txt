#1.Install virtual environment
sudo yum install python-virtualenv 

#2.Create a project folder and a venv folder within
mkdir myproject  
cd myproject
python3 -m venv venv

#3.Installing Flask
. venv/bin/activate
pip install Flask

echo"from flask import Flask
app= Flask(__name__)

@app.route("/")
def hello():
    return "Hello, World!"

if __name__ == "__main__":
    app.run(debug=true)>hello.py

#4.Export&run
export FLASK_APP=hello.py
python -m flask run